extends ../layout
include ../nav.pug
include abstractStory.pug 
block content 
    - const elements = [{name: "home", href: "/"}]
    +navBar(elements)
    if retrieved
        h1(style="text-align:center;") stories
        for i in stories 
            +abstractStory(i, true)
            hr
    else 
        p #{message}
